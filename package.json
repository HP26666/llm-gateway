{
  "name": "llm-gateway",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Local Anthropic-compatible LLM gateway with single-entry CLI + gateway runtime.",
  "scripts": {
    "start": "node main.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
