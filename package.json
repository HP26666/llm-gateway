{
  "name": "llm-gateway",
  "version": "3.3.0",
  "private": true,
  "type": "module",
  "description": "Local LLM gateway (Anthropic Messages + OpenAI Responses APIs) with single-entry CLI + gateway runtime.",
  "scripts": {
    "start": "node main.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
